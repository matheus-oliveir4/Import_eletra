# Local development can point to a separately configured API instance.
API_DEV_ORIGIN=http://localhost:4000

# Configure these values only in Vercel. Never use NEXT_PUBLIC_.
# The PostgreSQL URI and OIDC credentials belong only on the Hostinger VPS.
VPS_API_URL=
VPS_API_TOKEN=
